- hosts: all
  pre_tasks:
  - name: Include Variables
    ansible.builtin.include_vars: config.yml

  roles:
  - role: limesurvey

  post_tasks:
  - name: Ensure Limesurvey http is reachable (port 8080)
    ansible.builtin.wait_for:
      port: 8080
